# --- 数据库 ---
DATABASE_URL=
# PGSSL=require

# --- 加密密钥（必填）---
# openssl rand -hex 32 生成。部署后不要再改！
ENC_KEY=

# --- 管理员口令 ---
# 不设则使用默认口令 071926z.ai。生产环境建议改成你自己的。
ADMIN_TOKEN=071926z.ai

# --- 每个 Key 最多发给多少个选手（默认 10，发满标记为已发送）---
MAX_PER_KEY=10

# --- 库存阈值与通知（选填）---
LOW_STOCK_THRESHOLD=10
NOTIFY_WEBHOOK_URL=
NOTIFY_TYPE=generic

# PORT=8080
